syntax = "proto3";

package coreapp.domain.v1;

// DomainUpdatedEventData represents when a domain's configuration has been updated
message DomainUpdatedEventData {
  // The list of affected domain IDs
  repeated string id = 1;
}